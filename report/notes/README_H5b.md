# H5b install

Documentation/source-resolution stage only. No optimizer is run.

Copy into project root, preserving paths:

- `report/notes/H5b_comparator_source_resolution.md`
- `report/tables/H5b_comparator_source_matrix.csv`

Key freeze:

`AUTHOR_SOURCE_FIRST__SCHO_TABLE6_PARAMETER_PRECEDENCE_FOR_H5`

Critical identity:
`SHO` in the SCHO paper means **Sea-Horse Optimizer**, not Spotted Hyena Optimizer.

Next:
H5c1 — implement/test SCA only.
